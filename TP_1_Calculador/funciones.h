/** \brief pedir el primer operando al usuario.
 * \return el operando ingresado por el usuario.
 *
 */

float operando1(float);
/** \brief pedir el segundo operando al usuario.
 * \return el operando ingresado por el usuario.
 *
 */

float operando2(float);
/** \brief realizar la sumatoria de los operandos ingresados por el usuario.
 *
 * \param primer operando.
 * \param segundo operando.
 * \return la suma de los operandos ingreados.
 *
 */

float sumar (float,float);
/** \brief realizar la resta de los operandos ingresado por el usuario.
 *
 * \param primer operando.
 * \param segundo operando.
 * \return la resta de los operandos ingresados.
 *
 */

float restar (float,float);
/** \brief realizar la multiplicacion de los operandos inresadas por el usuario.
 *
 * \param multipicando.
 * \param multiplicador.
 * \return la multiplicacion de los operandos ingresados.
 *
 */

float multiplicacion (float,float);
/** \brief realizar la division de los operandos inresadas por el usuario.
 *
 * \param dividendo.
 * \param divisor.
 * \return la division de los operandos ingresados.
 *
 */

float division (float,float);
/** \brief realizar el factorial del primer operando ingresado por el usuario.
 *
 * \param primer operando a fatorear.
 * \return el factorial del operando.
 *
 */

float factorial (float);
